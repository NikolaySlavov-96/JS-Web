<section class="details-page">
    <h1>Details</h1>
    <article class="details-card">
        <article class="details-card-text">
            <h2>Title:<br />{{book.titleBook}}</h2>
            <h3>Author: {{book.author}}</h3>
            <h3>Stars: {{book.start}}</h3>
            <h3>Genre: {{book.genge}}</h3>
            <h3>{{book.bookReview}}</h3>

            <!-- If there is no registered user, do not display buttons-->
            {{#if username}}
                <div class="buttons">
                    {{#if option.isOwner}}
                        <!-- Only for registered user and author of the review -->
                        <a href="/catalog/edit/{{book._id}}" class="btn-edit">Edit</a>
                        <a href="/catalog/delete/{{book._id}}" class="btn-delete">Delete</a>
                    {{else}}
                        {{#if option.isWish}}
                            <!-- logged in user who has already wished book-->
                            <p class="wish-pub">You already added the book to your wish list</p>
                        {{else}}
                            <!-- logged in user who has not yet wished book-->
                            <a href="/catalog/wish/{{book._id}}" class="btn-wish">Wish to read</a>
                        {{/if}}
                    {{/if}}
                </div>
            {{/if}}
        </article>

        <article class="details-card-image">
            <img src="{{book.image}}" />
        </article>
    </article>
</section>